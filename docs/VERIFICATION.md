# Результати перевірки

Дата: 18.09.2026. Середовище: Windows, Node.js 24.18.0.

## Автоматично

`npm test`: **14 тестів пройдено**, 0 помилок.

- Каталог, пагінація, український пошук без урахування регістру, ціна, залишки та підкатегорії.
- Реєстрація, хеш пароля, сесія, дубль email, невірний пароль.
- Закриті маршрути та права адміністратора.
- CSRF-токен і джерело запиту.
- Обмеження кошика й ізоляція обраного між користувачами.
- Ціни із сервера, копія позицій у замовленні та повторна відправка одного запиту.
- Повернення залишків при скасуванні лише один раз.
- Два покупці претендують на останню одиницю: успішний лише один.
- Цикли категорій, заборона видалення непорожньої категорії.
- Створення, редагування й приховування товару без втрати історії.
- Відхилення файлу під виглядом PNG, приймання PNG та видача зображення.
- Блокування користувача відкликає сесію; адміністратор не блокує себе.
- Збереження даних після закриття й повторного відкриття SQLite.
- Вихід закриває серверну сесію.

`npm audit --omit=dev --audit-level=high`: відомих вразливостей на момент перевірки не знайдено.

## У браузері

- Пошук `ЛЯЛЬКА` знайшов дві відповідні іграшки.
- Додано товар у гостьовий кошик, зареєстровано тестового покупця; кошик перенісся до акаунту.
- Створено навчальне замовлення; у кабінеті відобразилися його позиції, сума та статус.
- Виконано вихід і вхід адміністратора.
- Через адмінку створено товар; він з'явився у таблиці з правильною ціною та залишком.
- Статус замовлення змінено з «Нове» на «Підтверджене».
- Перевірено відображення при ширині 1280, 375 і 320 px; горизонтального переповнення документа не було.
- На перевірених сторінках консоль браузера не показала помилок JavaScript.

Дані ручної перевірки створювалися в окремій робочій копії. У кінцеву папку передається початковий демонстраційний каталог без цих тестових замовлень.

Обмеження: перевірка у браузері Codex на Chromium. Окремі запуски Safari, Firefox і реального мобільного пристрою не виконувалися. Реальні платежі та доставка не інтегровані.
